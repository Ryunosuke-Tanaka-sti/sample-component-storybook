type Props = {
  children: React.ReactNode;
};

export const {{pascalCase name}} = (props:Props) => {
    const { children } = props;
    return (
        <>
            {children}
        </>
    );
};


